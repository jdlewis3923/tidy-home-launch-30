# Tidy roadmap

## In progress

1. Secrets
   - CHECKR_API_KEY — pending (user will add when available)
   - CHECKR_PACKAGE — pending (user will add when available)
   - CHECKR_WEBHOOK_SECRET — pending (user will add when available)
   - BREVO_TEMPLATE_WELCOME_T1 — adding now
   - TWILIO_FROM_NUMBER — adding now
2. /apply Indeed-style gates (bilingual, insurance, FL license + expiry)
3. Badge admin panel (/admin/badges, status log, suspended/revoked states)
4. Command "Needs attention" panel (orphan/retired-SKU customers)
5. Site gate header reads real `site_live` value (DARK / WAITLIST / LIVE)
